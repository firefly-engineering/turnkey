[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "turnkey-cfg"
version = "0.1.0"
description = "Cargo cfg() expression parser and evaluator"
requires-python = ">=3.11"

[tool.hatch.build.targets.wheel]
packages = ["turnkey"]
