[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "turnkey-buildsystem"
version = "0.1.0"
description = "Build-system-agnostic native library generation"
requires-python = ">=3.11"

[tool.hatch.build.targets.wheel]
packages = ["turnkey"]
