[project]
name = "hello-python"
version = "0.1.0"
description = "Multi-language fixture Python app"
requires-python = ">=3.9"
dependencies = [
    "six>=1.16.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
